# 🚀 Быстрый старт - AI Security Test Assistant

## ✅ Что уже сделано:
- ✅ Backend сервер настроен и работает
- ✅ Frontend доступен через `http://localhost:8000/`
- ✅ Все зависимости установлены
- ✅ Шаблоны и форматирование кода готовы

## 📋 Что нужно сделать для полной работы:

### 1. Убедитесь, что сервер запущен

```powershell
# Если сервер не запущен, выполните:
.venv\Scripts\python.exe -m uvicorn backend.main:app --reload

# Или используйте скрипт:
.\start_server.bat
```

### 2. Откройте проект в браузере

Откройте в браузере: **http://localhost:8000/**

Вы увидите интерфейс в стиле кибербезопасности! 🔒

### 3. Настройте AI провайдер (опционально, но рекомендуется)

Для работы генерации тест-кейсов и кода нужен AI провайдер.

#### Вариант A: OpenAI (платный, но быстрый)

Создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_MODEL=gpt-4o-mini
AI_PROVIDER=openai
```

Получить API ключ: https://platform.openai.com/api-keys

#### Вариант B: Ollama (бесплатный, локальный)

1. Установите Ollama: https://ollama.ai/
2. Установите модель:
   ```bash
   ollama pull llama3
   ```
3. Создайте файл `.env`:
   ```env
   AI_PROVIDER=ollama
   OLLAMA_BASE_URL=http://localhost:11434
   OLLAMA_MODEL=llama3
   ```

### 4. Проверьте работу

1. Откройте http://localhost:8000/
2. Попробуйте сгенерировать тест-кейсы:
   - Введите описание: "Логин с email и паролем"
   - Нажмите "Сгенерировать тест-кейсы"
3. Используйте шаблоны:
   - Выберите шаблон "auth" в dropdown
   - Заполните параметры
   - Нажмите "Сгенерировать код"

## 🎯 Основные функции:

### Генерация тест-кейсов
- Введите описание фичи
- Выберите провайдер ИИ (OpenAI/Ollama) и модель
- Выберите формат (JSON/Markdown)
- Получите готовые тест-кейсы

### Генерация автотестов
- Используйте шаблоны (auth, form, list, crud)
- Или введите тест-кейс вручную
- Выберите язык (TypeScript/JavaScript/Python)
- Код автоматически отформатируется

### AI Review
- Вставьте сгенерированный код
- Получите анализ и предложения по улучшению

## 🔧 Дополнительные настройки (опционально):

### GitHub интеграция
```env
GITHUB_TOKEN=ghp_your-token-here
DEFAULT_REPO_ROOT=.
```

### Форматирование кода (автоматическое)
- **TypeScript/JavaScript**: Установите prettier (автоматически через npx)
- **Python**: Установите black или ruff
  ```bash
  pip install black
  # или
  pip install ruff
  ```

## 📝 Полезные ссылки:

- API документация: http://localhost:8000/docs
- Health check: http://localhost:8000/health
- Список шаблонов: http://localhost:8000/templates

## ❓ Проблемы?

### Сервер не запускается
- Проверьте, что Python установлен: `python --version`
- Активируйте виртуальное окружение: `.venv\Scripts\Activate.ps1`
- Установите зависимости: `pip install -r requirements.txt`

### Frontend не открывается
- Убедитесь, что сервер запущен на порту 8000
- Проверьте: http://localhost:8000/health
- Если не работает, откройте файл напрямую: `frontend/index.html`

### AI не работает
- Проверьте `.env` файл
- Для OpenAI: убедитесь, что API ключ валидный
- Для Ollama: убедитесь, что Ollama запущен (`ollama serve`)

## 🎉 Готово!

Теперь вы можете:
- ✅ Генерировать тест-кейсы из описания
- ✅ Создавать автотесты на Playwright
- ✅ Использовать готовые шаблоны
- ✅ Получать AI-ревью кода
- ✅ Сохранять тесты локально или в GitHub

Удачи! 🚀

